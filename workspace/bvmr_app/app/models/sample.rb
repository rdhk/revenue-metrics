class Sample < ActiveRecord::Base
	# def parse(file_path)
 #    file = Roo::Excel.new(file_path)

 #    (1..file.last_row).each do |i|
 #      row = file.row(i)
 #      sample = self.new(order_date: row[0], sales: row[1]) # Depends on your needs
 #      sample.save! if sample.valid? 
 #    end
 #  end
end

